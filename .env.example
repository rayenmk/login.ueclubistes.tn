# UE Clubiste - Example environment variables

# Frontend (public, safe to use only publishable/anon key)
SUPABASE_URL=https://<your-project-ref>.supabase.co
SUPABASE_ANON_KEY=sb_publishable_XXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Edge Functions / Server (keep secrets private)
# Option A (legacy): single service role key
# SUPABASE_SERVICE_ROLE_KEY=svc_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Option B (recommended): JSON object of secret keys (the functions parse this)
# SUPABASE_SECRET_KEYS={"service_role":"svc_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX","other":"..."}

# Optional: Admin bearer token for local testing (do NOT commit)
# ADMIN_TEST_TOKEN=eyJhbGciOiJI... 
